# jsutils 封装

lodash 是很常用的 js 通用操作的一个库。

1. 但是一些函数我们日常使用到，但是 lodash 里面没有，所以我们可以封装。
2. JavaScript 一些新特性，比较实用，但是因为项目框架比较老，我们在项目里面用不了，所以自己封装一下就能提前实用了。
3. 还有我们不止学习一种编程语言。其他语言里面比较方便的操作，可以考虑搬到 js 里面来。

import CodeBlock from '@theme/CodeBlock'

## 数组

import ArrayUtil from '!!raw-loader!@site/src/utils/jsutils/array.ts'

<CodeBlock language="ts">{ArrayUtil}</CodeBlock>
